import { useRef } from "react";
import { useReactToPrint } from "react-to-print";

interface Props {
  //   printData;
  data;
}
export default function PrintSales({ data }: Props) {
  return (
    <>
      <div>
        <div>
          <div>Hello Print</div>
        </div>
      </div>
    </>
  );
}
